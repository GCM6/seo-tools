# UX 优化执行实现笔记(2026-07-19)

依据:`2026-07-19-ux-audit-optimization-plan.md`。编排模式执行,本文件记录拍板决策、波次进度与各任务偏离项。

## 拍板决策
- A3 复测口径:**保留顺延 + 复测计划卡**(方案 c)——逻辑不变(最新一次执行 +28 天),output 页新增复测计划卡(明示到期日与口径、可调整日期、可立即发起复测),"已执行"可撤销。(用户 2026-07-19 选定)
- 执行范围:**全部 A-D 四包**。(用户 2026-07-19 选定)

## 波次
- 第 1 波(并行,进行中):A1 reviewGate 接线 / A2 重访入口+子页面包屑 / B1+B2 交付物可执行化(含 output 页返回链接)
- 第 1.5 波(并行,进行中):C1 报告三段式重排 / C3 徽章去重 / C5 PresenceMap 双区区分 / D2 语义色收敛 / D3 路由级三态
- 第 2 波(待第 1 波落地):A3+A4 output 工作台(复测计划卡+执行撤销+prompt 重生成)/ C4 screen2 侧目录+D5 RunProgress 按钮样式 / C6 关键词表排序合并 / D1+D4 三子页 token 迁移+空态统一
- 第 3 波(待 C1 与 D1 落地):C2 术语翻译层(Term 组件,应用于重排后的 ReportView 与迁移后的 site 页)
- 收口:全量 vitest + build + 跨任务集成检查 + git 快照对比

## 已知风险与缓解
- messages/zh.json、en.json 多代理并发编辑:各任务书已要求"i18n 放最后、编辑前重读、唯一锚点小步 Edit";收口时人工检查 JSON 合法性。
- globals.css 双代理分区编辑(C5 只动 .map-* 段,D2 只动 @theme/token 区)。
- 陷阱备忘:审查阶段曾发生只读代理越权删除 6 个孤儿死代码文件(ReportPanel/DeliveryCard/DeliveryExportActions/PromptCard 及测试),未提交修改不可恢复但不破坏构建(1174 测试绿);本轮所有任务书已加"禁删除/禁 commit"硬约束,每波收口对比 git 快照。

## 完成情况

| 任务 | 状态 | 验收 |
|---|---|---|
| A1 reviewGate 接线 | ✅ 已验收(编排者亲验 diff) | RunProgress 4 用例绿;死按钮在 gate 存在时不再渲染 |
| A2 重访入口+子页面包屑 | ✅ 已验收 | RunHistory 8 用例绿;调用方传参由编排者直通补齐 |
| C3 徽章去重+就近图例 | ✅ 已验收 | FindingList 5 用例绿 |
| C5 PresenceMap 双区区分 | ✅ 已验收 | PresenceMap 11 用例绿 |
| D2 语义色收敛 | ✅ 已验收 | 裸色 grep 零命中;18 用例绿 |
| D3 路由级三态 | ✅ 已验收 | 新增 9 个 loading/error 文件 |
| B 包 交付物可执行化 | 🔁 中断后续做中 | 前一代理额度耗尽于半途 |
| C1 报告三段式重排 | 🔁 中断后修复中 | 半成品:重复 section + 方法与范围丢失 |
| C4 主页导航收敛 | ⏳ 进行中 | |
| C6 关键词表排序合并 | ⏳ 进行中 | |
| D1+D4 三子页迁移(含 P1-10 统计卡着色) | ⏳ 进行中 | |
| B 包 交付物可执行化 | ✅ 已验收 | 40 用例复跑绿;受影响页面清单+证据摘要 |
| C1 报告三段式重排 | ✅ 已验收(中断后修复) | 9 section 唯一、方法节还原、14 用例复跑绿 |
| C4 主页导航收敛 | ✅ 已验收 | 9 个锚点亲验存在、无死链 |
| C6 关键词表排序合并 | ✅ 已验收 | 44 用例绿;合并单表 8 列+服务端 orderBy |
| D1+D4 三子页迁移 | ✅ 已验收 | 裸色零残留、表格容器+空态组件就位 |
| A3+A4 output 工作台 | ✅ 已验收 | 31 用例复跑绿;撤销分支+两处接线亲验 |
| Map 加固(编排者追加) | ✅ 已验收 | RSC 边界改传普通对象,消除未验证假设 |
| C2 术语翻译层 | ✅ 已验收 | Term 组件 + 15 条术语解释;18 用例绿 |

| D5 RunProgress 按钮样式 | ✅ 已验收(补漏) | `.rp-action`/`.rp-action-retry`,走 token 无写死色 |
| P1-4 健康分↔严重度解释 | ✅ 已验收(补漏) | details 内加人话说明,原始 breakdown 保留 |

## 清点发现的方案编排疏漏(编排者自省)

首轮派单时遗漏三项,均非执行代理问题:
1. **D5**——方案 D 包明确列出,派 C4 时漏带。已补。
2. **P1-4**(健康分与严重度两套体系无换算解释)——只写进问题表、未编 C 号,派单时跟着漏。已补。
3. **P1-9**(关键词页/站点页无"下一步"收敛摘要)——同样未编号。**未做**:它不是修断裂而是新增内容块,涉及产品判断(摘要说什么、阈值怎么定、会不会沦为又一条被忽略的横幅),建议用户真机走查后再决定。

教训:方案文档里"问题清单表"与"实施包编号"必须一一映射,未编号的问题项在派单阶段会系统性丢失。

## 仍未处理的既有遗留(不在 A-D 包内)

- `RulesAdminClient.tsx` 105/108/175-176 行三处 emerald/amber 散色未收敛(D2 授权锚点外)。
- ~~**`Skeleton` 组件使用的 `.skeleton` 类在 globals.css 中从未定义**~~ → **已修复(编排者直通)**。核实属实(observed:组件渲染 `className="skeleton"`,globals.css 零定义,骨架屏实为不可见空 div)。补法:复用既有 `fx-shimmer` 关键帧,新增 `.skeleton` 规则(底色 `--ds-surface-2`、圆角 `--rd-sm`、扫光用 `color-mix` 走 `--ds-surface-3`,尺寸仍由组件内联 style 提供),并把 `.skeleton` 加入既有 `prefers-reduced-motion` 兜底列表。验证:花括号配平 960/960、构建 exit 0。

## 收口验证(编排者亲自执行)

- `npx vitest run` → **156 文件 / 1242 测试全绿**(起点 1174,本轮净增 68 个测试)
- `npx tsc --noEmit` → exit 0,零错误
- `npx next build` → exit 0,26 个静态页生成成功
- zh/en JSON → 合法
- git 快照对比 → 12 个执行任务**无一新增删除**,删除清单仍是审查阶段那 6 个孤儿文件;变更规模 38 文件改动 + 31 个新文件

## 编排者追加的加固任务

C6 把 `KeywordTable` 改为 Client Component 后,两个 Server Component 调用方需跨 RSC 边界传 `Map`。C6 仅做源码级判断、未运行时验证;编排者独立 grep 亦无法廉价证实该序列化路径受支持。**处理方式:不去论证假设,而是消除对它的依赖**——`keywordText` 改为 `Object.fromEntries` 产出的普通对象,组件内部(client 侧)再重建 Map。已验证 28 用例绿、9 章节完好。

## 各任务偏离项汇总

- **A1**:放开了 reviewing 态的 RunProgress 渲染条件(原为 `status !== 'reviewing'` 整体隐藏),否则 totalCount=0 边界仍无出口。副作用:pending>0 时顶部 gate CTA 与底部引导横幅双入口并存——编排者裁决**保留**(长页面双入口利大于弊)。
- **A2**:守约束停在调用方接线前一步(`projects/[id]/page.tsx` 不在其允许清单),新增 label 设为可选 prop。**编排者直通补齐两行传参**(viewOutput/confirmRecs),新链接现已实际渲染。
- **D2**:①顺带把 `BrandFactRow` 中同一三元表达式内的 `#6b7280` 一并归 `--ds-muted`(同系统同逻辑,已接受);②`RulesAdminClient` 中 105/108/175-176 行另有 emerald/amber 散色在授权锚点外,**未动,留作后续清理**。
- **D3**:发现既有缺口——`Skeleton` 组件使用的 `.skeleton` 类在 globals.css 中未定义(全仓确认),骨架屏可能无灰底动画。非本轮引入,**留作后续清理**。
- **C5/C3/D3**:均报告 messages/*.json 存在多代理并发写入,各自用"重读+唯一锚点小步 Edit"规避,事后 JSON 合法性校验通过(编排者独立复核:zh/en 均 JSON OK)。
- **中断事故**:B 包与 C1 两个代理因会话额度(19:00 重置)在半途终止。C1 留下破坏性半成品(section 重复 + 整节丢失),已派专任务按 HEAD 基线修复;B 包留下的部分改动未破坏测试,已派任务续做。教训:长任务代理应在任务书中要求"每完成一个可验证子步骤即自检一次",避免中断落在不自洽状态。
