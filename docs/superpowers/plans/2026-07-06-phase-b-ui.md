# Phase B UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让已接通但「看不见、连不上」的 Phase B 数据可见可操作——设置页（数据源状态矩阵 + GSC 一键连接 + 站点 URL 设置）、关键词现状 tab（复用报告 §4 渲染）、avgRank 卡通真（展示量加权平均排名）。

**Architecture:** 三块弱耦合切片：① avgRank 是纯函数 + 采集器证据 payload 补 `avgPosition` 字段 + deriveAvgRank 择证据（后端小改）；② 设置页 = 纯逻辑状态矩阵 + Server 页 + Client 交互 + 一个 GSC 站点路由 + callback redirect 改向 + 两个仓库读写器 + Shell 顶栏入口；③ 关键词 tab = 把报告 §4 关键词渲染抽成共享 `KeywordTable` 组件，报告页与新 tab 同用。

**Tech Stack:** TypeScript · Next.js 16 App Router（async `params`/`searchParams` 为 Promise，Server Component 默认，交互用 `'use client'`）· React 19 · libSQL/Drizzle · next-intl · Vitest。

## Global Constraints

- **编码前必读**：写/改任何 `.ts`/`.tsx` 前先调用 `veris-coding` skill（React 19 + Next 16：async params、无 forwardRef、Server/Client 边界）。
- **直接 main 开发，commit per task**（沿用 SP2/A–F 约定，不建分支）。
- **证据护城河**：avgRank 显真值时必须 measured **L4**、挂在 `gsc` 证据上、可溯源（复用既有卡逻辑）；无数据照旧 pending。不得凭空造值。
- **i18n 锁步**：`messages/en.json` 与 `messages/zh.json` 顶层命名空间集合与键结构必须一致，只值不同。新增 `settings`、`keywords` 命名空间 + `screen2.keywordsLink` + `common.settingsLink`。
- **纯函数无 `Date.now()`/`new Date()`**（`impressionWeightedAvgPosition`、`buildDataSourceStatuses`）。
- **语言规范**：UI 文案中文（经 i18n）、报错中文；标识符/路由/字段英文；注释中文。
- **单项目 V0**：无项目选择器；`getPrimaryProject()` 取唯一/首个项目；无项目时空态引导「先创建分析」。
- **AI 探针只有 4 家**：OpenAI / Perplexity / Gemini / DeepSeek（无 Anthropic 探针）——env 键 `OPENAI_API_KEY`/`PERPLEXITY_API_KEY`/`GEMINI_API_KEY`/`DEEPSEEK_API_KEY`。
- **验证门槛**：`npx tsc --noEmit` 0 · `npm run lint` 0 error · `npm test` 全绿 · `npm run build` 通过。

## File Structure

| 文件 | 动作 | 职责 |
|---|---|---|
| `lib/gsc/avg-position.ts` (+test) | 建 | `impressionWeightedAvgPosition(rows)`——纯函数 |
| `lib/inngest/collect-evidence.ts` | 改 | query 维 gsc 证据 payload 补 `avgPosition` |
| `lib/diagnostics.ts` | 改 | `deriveAvgRank` 择带 `avgPosition` 的 gsc 证据 |
| `lib/diagnostics.test.ts` (+/建) | 改/建 | deriveAvgRank measured/pending 测试 |
| `lib/settings/data-sources.ts` (+test) | 建 | `buildDataSourceStatuses(env, gsc)`——纯函数状态矩阵 |
| `lib/repositories/index.ts` | 改 | `getPrimaryProject()`、`setGscSiteUrl(projectId, siteUrl)` |
| `app/api/gsc/site/route.ts` | 建 | POST 设 gscSiteUrl |
| `app/api/gsc/callback/route.ts` | 改 | 成功 redirect → `/settings?gsc=connected` |
| `components/Shell.tsx` | 改 | 顶栏加设置链接 |
| `app/[locale]/settings/page.tsx` + `SettingsClient.tsx` | 建 | 状态矩阵 + GSC 连接 + 站点 URL 表单 |
| `components/KeywordTable.tsx` | 建 | 抽自报告 §4（metrics + gaps + 空态），两处同用 |
| `app/[locale]/runs/[id]/report/page.tsx` | 改 | §4 改调 KeywordTable |
| `app/[locale]/runs/[id]/keywords/page.tsx` | 建 | 关键词 tab（页 chrome + KeywordTable + 空态 CTA） |
| `app/[locale]/runs/[id]/page.tsx` | 改 | Screen2 加 keywordsLink |
| `messages/{en,zh}.json` | 改 | `settings`、`keywords` 命名空间 + `screen2.keywordsLink` + `common.settingsLink` |

---

## Task 1: avgRank 卡通真

**Files:**
- Create: `lib/gsc/avg-position.ts`、`lib/gsc/avg-position.test.ts`
- Modify: `lib/inngest/collect-evidence.ts:434-444`、`lib/diagnostics.ts:65-72`
- Test: `lib/diagnostics.test.ts`（若无则建）

**Interfaces:**
- Produces: `impressionWeightedAvgPosition(rows: { impressions: number; position: number }[]): number | null`；query 维 gsc 证据 payload 变为 `{ dimension: 'query', rows, avgPosition }`；`deriveAvgRank` 择含 `avgPosition` 的 gsc 证据。

- [ ] **Step 1: 写平均排名纯函数失败测试**

写入 `lib/gsc/avg-position.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { impressionWeightedAvgPosition } from './avg-position'

describe('impressionWeightedAvgPosition', () => {
  it('展示量加权：高展示词主导', () => {
    // (2*100 + 10*10)/110 = 300/110 = 2.727 → 2.7
    expect(impressionWeightedAvgPosition([
      { position: 2, impressions: 100 },
      { position: 10, impressions: 10 },
    ])).toBe(2.7)
  })
  it('空行或零展示 → null（无信号）', () => {
    expect(impressionWeightedAvgPosition([])).toBeNull()
    expect(impressionWeightedAvgPosition([{ position: 5, impressions: 0 }])).toBeNull()
  })
  it('四舍五入到一位小数', () => {
    expect(impressionWeightedAvgPosition([{ position: 3.14159, impressions: 5 }])).toBe(3.1)
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run lib/gsc/avg-position.test.ts`
Expected: FAIL — `Cannot find module './avg-position'`。

- [ ] **Step 3: 实现纯函数**

写入 `lib/gsc/avg-position.ts`：

```ts
// GSC 展示量加权平均排名：sum(position_i * impressions_i) / sum(impressions_i)。
// GSC「平均排名」的标准口径。总展示量 <= 0（空行/零展示）→ null（无信号，卡保持 pending）。
export function impressionWeightedAvgPosition(
  rows: { impressions: number; position: number }[],
): number | null {
  let weighted = 0
  let totalImpr = 0
  for (const r of rows) {
    const impr = typeof r.impressions === 'number' ? r.impressions : 0
    const pos = typeof r.position === 'number' ? r.position : 0
    weighted += pos * impr
    totalImpr += impr
  }
  if (totalImpr <= 0) return null
  return Math.round((weighted / totalImpr) * 10) / 10
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run lib/gsc/avg-position.test.ts`
Expected: PASS（3 用例）。

- [ ] **Step 5: 采集器 query 维证据 payload 补 avgPosition**

`lib/inngest/collect-evidence.ts` 顶部 import：`import { impressionWeightedAvgPosition } from '@/lib/gsc/avg-position'`。

把 434-444 段改为（加一行算 avgPosition + payload 补字段）：

```ts
      // query 维证据：K01/K02 的取数锚。keyword_metrics 也引用它作 evidenceId。
      const queryEvId = `ev_${crypto.randomUUID()}`
      const avgPosition = impressionWeightedAvgPosition(gsc.queryRows)
      const queryRaw = JSON.stringify(gsc.queryRows)
      await step.run('persist-gsc-query', () =>
        deps.createEvidenceArtifact({
          id: queryEvId, projectId, runId, type: 'gsc', claimLevel: 'L4', source: siteUrl,
          request: { dimension: 'query', ...gsc.range },
          payload: { dimension: 'query', rows: gsc.queryRows, avgPosition },
          rawText: queryRaw, rawHash: sha256Hex(queryRaw),
        }),
      )
```

（`gsc.queryRows` 每行是 `GscRow`，含 `impressions`/`position`，见 `lib/gsc/search-analytics.ts:7-13`。）

- [ ] **Step 6: deriveAvgRank 择带 avgPosition 的 gsc 证据 —— 先写测试**

`lib/diagnostics.test.ts`（若不存在则建）追加：

```ts
import { describe, it, expect } from 'vitest'
import { deriveStatCards } from './diagnostics'

const gscEv = (id: string, payload: unknown) => ({ id, type: 'gsc', claimLevel: 'L4', payload })

describe('deriveAvgRank', () => {
  it('gsc 证据带 avgPosition → measured L4', () => {
    const cards = deriveStatCards([gscEv('ev1', { dimension: 'query', rows: [], avgPosition: 4.2 })])
    const avg = cards.find((c) => c.key === 'avgRank')!
    expect(avg.state).toBe('measured')
    expect(avg).toMatchObject({ state: 'measured', value: '4.2', level: 'L4', evidenceId: 'ev1' })
  })
  it('多条 gsc 证据时择带 avgPosition 的那条（queryPage 无 avgPosition）', () => {
    const cards = deriveStatCards([
      gscEv('evQP', { dimension: 'queryPage', rows: [] }),
      gscEv('evQ', { dimension: 'query', rows: [], avgPosition: 3 }),
    ])
    expect(cards.find((c) => c.key === 'avgRank')).toMatchObject({ state: 'measured', evidenceId: 'evQ' })
  })
  it('无 gsc / 无 avgPosition → pending', () => {
    expect(deriveStatCards([]).find((c) => c.key === 'avgRank')).toEqual({ key: 'avgRank', state: 'pending', reason: 'gsc' })
    expect(deriveStatCards([gscEv('e', { dimension: 'query', rows: [] })]).find((c) => c.key === 'avgRank'))
      .toEqual({ key: 'avgRank', state: 'pending', reason: 'gsc' })
  })
})
```

- [ ] **Step 7: 跑测试确认失败**

Run: `npx vitest run lib/diagnostics.test.ts`
Expected: FAIL —「多条 gsc」用例挂（现 `deriveAvgRank` 用 `pick` 取第一条 gsc，取到无 avgPosition 的 queryPage → pending，与断言 measured 冲突）。

- [ ] **Step 8: 改 deriveAvgRank**

`lib/diagnostics.ts:65-72` 替换为：

```ts
// 平均自然排名来自 GSC。择带 avgPosition 的 gsc 证据（一次采集写 query + queryPage 两条 gsc，仅 query 维带 avgPosition）。
function deriveAvgRank(evidence: EvidenceLike[]): StatCard {
  const gsc = evidence.find((e) => e.type === 'gsc' && num(e.payload, 'avgPosition') !== undefined)
  if (gsc) {
    const avgPosition = num(gsc.payload, 'avgPosition')!
    return { key: 'avgRank', state: 'measured', value: String(avgPosition), level: gsc.claimLevel as EvidenceLevel, evidenceId: gsc.id }
  }
  return { key: 'avgRank', state: 'pending', reason: 'gsc' }
}
```

- [ ] **Step 9: 跑测试 + tsc + 全量**

Run: `npx vitest run lib/gsc/avg-position.test.ts lib/diagnostics.test.ts && npx tsc --noEmit && npm test`
Expected: 焦点全绿；tsc 0；全量绿（若既有 collect-evidence 测试断言 gsc payload 形状，更新为含 `avgPosition`——payload 是叠加字段，多半不影响）。

- [ ] **Step 10: Commit**

```bash
git add lib/gsc/avg-position.ts lib/gsc/avg-position.test.ts lib/inngest/collect-evidence.ts lib/diagnostics.ts lib/diagnostics.test.ts
git commit -m "feat(phase-b-ui): avgRank 卡通真——展示量加权平均排名写进 gsc 证据 payload + deriveAvgRank 择证据"
```

---

## Task 2: 数据源状态矩阵纯逻辑

**Files:**
- Create: `lib/settings/data-sources.ts`、`lib/settings/data-sources.test.ts`

**Interfaces:**
- Produces:
  - 类型 `DataSourceKey = 'gsc'|'googleCse'|'aiProbe'|'dataforseo'|'render'|'psi'|'publicCorpora'`
  - `DataSourceStatus { key: DataSourceKey; configured: boolean; connected?: boolean; detail?: string }`
  - `GscConnection { gscAppConfigured: boolean; gscConnected: boolean; gscSiteUrl: string | null }`
  - `buildDataSourceStatuses(env: Record<string, string | undefined>, gsc: GscConnection): DataSourceStatus[]`
- Consumes（下游）：Task 3 设置页调用。

- [ ] **Step 1: 写失败测试**

写入 `lib/settings/data-sources.test.ts`：

```ts
import { describe, it, expect } from 'vitest'
import { buildDataSourceStatuses } from './data-sources'

const gscConn = { gscAppConfigured: true, gscConnected: true, gscSiteUrl: 'sc-domain:x.com' }

describe('buildDataSourceStatuses', () => {
  it('空 env：GSC 连接态来自入参，其余按 env 判定，PSI/语料恒 configured', () => {
    const rows = buildDataSourceStatuses({}, gscConn)
    const by = Object.fromEntries(rows.map((r) => [r.key, r]))
    expect(by.gsc).toMatchObject({ configured: true, connected: true, detail: 'sc-domain:x.com' })
    expect(by.dataforseo.configured).toBe(false)
    expect(by.googleCse.configured).toBe(false)
    expect(by.render.configured).toBe(false)
    expect(by.psi.configured).toBe(true)
    expect(by.publicCorpora.configured).toBe(true)
    expect(by.aiProbe).toMatchObject({ configured: false, detail: '0/4' })
  })
  it('env 就绪：dataforseo/cse/render/探针置真，探针计数正确', () => {
    const rows = buildDataSourceStatuses(
      { DATAFORSEO_LOGIN: 'a', DATAFORSEO_PASSWORD: 'b', GOOGLE_CSE_API_KEY: 'k', GOOGLE_CSE_CX: 'c',
        CLOUDFLARE_ACCOUNT_ID: 'x', CLOUDFLARE_API_TOKEN: 'y', OPENAI_API_KEY: 'o', GEMINI_API_KEY: 'g' },
      gscConn,
    )
    const by = Object.fromEntries(rows.map((r) => [r.key, r]))
    expect(by.dataforseo.configured).toBe(true)
    expect(by.googleCse.configured).toBe(true)
    expect(by.render.configured).toBe(true)
    expect(by.aiProbe).toMatchObject({ configured: true, detail: '2/4' })
  })
  it('GSC app 未配（env 无 OAuth）→ configured false', () => {
    const rows = buildDataSourceStatuses({}, { gscAppConfigured: false, gscConnected: false, gscSiteUrl: null })
    expect(rows.find((r) => r.key === 'gsc')).toMatchObject({ configured: false, connected: false })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest run lib/settings/data-sources.test.ts`
Expected: FAIL — `Cannot find module './data-sources'`。

- [ ] **Step 3: 实现**

写入 `lib/settings/data-sources.ts`：

```ts
// 数据源状态矩阵（设置页）。纯函数：env 记录 + 本项目 GSC 连接态入参 → 各源状态。
export type DataSourceKey =
  | 'gsc' | 'googleCse' | 'aiProbe' | 'dataforseo' | 'render' | 'psi' | 'publicCorpora'

export interface DataSourceStatus {
  key: DataSourceKey
  configured: boolean          // 环境/服务可用
  connected?: boolean          // 本项目已授权（仅 GSC）
  detail?: string              // 附加信息（GSC 站点 URL / 已配探针数）
}

export interface GscConnection {
  gscAppConfigured: boolean    // isGscConfigured()——OAuth app 环境级
  gscConnected: boolean        // 本项目 settings.gscConnected
  gscSiteUrl: string | null
}

const AI_PROVIDER_ENVS = ['OPENAI_API_KEY', 'PERPLEXITY_API_KEY', 'GEMINI_API_KEY', 'DEEPSEEK_API_KEY']

export function buildDataSourceStatuses(
  env: Record<string, string | undefined>,
  gsc: GscConnection,
): DataSourceStatus[] {
  const aiCount = AI_PROVIDER_ENVS.filter((k) => !!env[k]).length
  return [
    { key: 'gsc', configured: gsc.gscAppConfigured, connected: gsc.gscConnected, detail: gsc.gscSiteUrl ?? undefined },
    { key: 'googleCse', configured: !!(env.GOOGLE_CSE_API_KEY && env.GOOGLE_CSE_CX) },
    { key: 'aiProbe', configured: aiCount > 0, detail: `${aiCount}/4` },
    { key: 'dataforseo', configured: !!(env.DATAFORSEO_LOGIN && env.DATAFORSEO_PASSWORD) },
    { key: 'render', configured: !!(env.CLOUDFLARE_ACCOUNT_ID && env.CLOUDFLARE_API_TOKEN) },
    { key: 'psi', configured: true },
    { key: 'publicCorpora', configured: true },
  ]
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest run lib/settings/data-sources.test.ts`
Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add lib/settings/data-sources.ts lib/settings/data-sources.test.ts
git commit -m "feat(phase-b-ui): 数据源状态矩阵纯逻辑 buildDataSourceStatuses"
```

---

## Task 3: 设置页 + GSC 站点路由 + callback 改向 + 仓库读写 + Shell 入口 + i18n

**Files:**
- Modify: `lib/repositories/index.ts`（+`getPrimaryProject`、`setGscSiteUrl`）
- Create: `app/api/gsc/site/route.ts`
- Modify: `app/api/gsc/callback/route.ts`（成功 redirect 目标）
- Modify: `components/Shell.tsx`
- Create: `app/[locale]/settings/page.tsx`、`app/[locale]/settings/SettingsClient.tsx`
- Modify: `messages/en.json`、`messages/zh.json`

**Interfaces:**
- Consumes: Task 2 `buildDataSourceStatuses`、`DataSourceStatus`；`isGscConfigured`（`@/lib/gsc/oauth`）；`getProjectSettings`。
- Produces: `getPrimaryProject(): Promise<Project | null>`、`setGscSiteUrl(projectId, siteUrl): Promise<...>`；`POST /api/gsc/site {projectId, siteUrl}`；`/[locale]/settings` 页。

- [ ] **Step 1: 仓库加 getPrimaryProject / setGscSiteUrl**

`lib/repositories/index.ts`——确认顶部已 import `asc`（Phase A 已用）与 `projects`、`projectSettings`。在合适处追加：

```ts
// 单项目 V0：取唯一/首个项目（按创建时间）。无项目返回 null。
export const getPrimaryProject = async () => {
  const rows = await db.select().from(projects).orderBy(asc(projects.createdAt)).limit(1)
  return rows[0] ?? null
}

// GSC 站点 URL 单独写（连接后设，闭合采集器 gscConnected+refreshToken+siteUrl 条件）。
export const setGscSiteUrl = (projectId: string, siteUrl: string) =>
  db.update(projectSettings).set({ gscSiteUrl: siteUrl }).where(eq(projectSettings.projectId, projectId))
```

- [ ] **Step 2: GSC 站点路由**

写入 `app/api/gsc/site/route.ts`：

```ts
import { NextResponse } from 'next/server'
import { setGscSiteUrl } from '@/lib/repositories'

export async function POST(req: Request) {
  const body = (await req.json().catch(() => ({}))) as { projectId?: string; siteUrl?: string }
  if (!body.projectId) return NextResponse.json({ error: 'project_id_required' }, { status: 422 })
  if (!body.siteUrl?.trim()) return NextResponse.json({ error: 'site_url_required' }, { status: 422 })
  await setGscSiteUrl(body.projectId, body.siteUrl.trim())
  return NextResponse.json({ ok: true, siteUrl: body.siteUrl.trim() })
}
```

- [ ] **Step 3: callback 成功 redirect 改向设置页**

Read `app/api/gsc/callback/route.ts`，把成功分支里 redirect 的目标 URL（当前形如 `/?gsc=connected&projectId=<id>`）改为 `/settings?gsc=connected`（去掉 projectId——设置页用 `getPrimaryProject()` 解析；next-intl 中间件补 locale 前缀）。**只改这一个 URL 字符串，保留 redirect 机制与其余错误分支不动。**

- [ ] **Step 4: Shell 顶栏加设置链接**

`components/Shell.tsx` 顶部 import：`import Link from 'next/link'`。在 `.target` div 内 `<LocaleSwitch />`（约 40 行）前插入：

```tsx
      <Link href={`/${locale}/settings`} className="settings-link">{t('settingsLink')}</Link>
      <LocaleSwitch />
```

（Shell 是 Server Component，用 `getTranslations('common')`，`next/link` 可在 server 组件用。）

- [ ] **Step 5: i18n 加 settings 命名空间 + common.settingsLink（en + zh 锁步）**

`messages/zh.json`：`common` 内加 `"settingsLink": "设置"`；顶层加：

```json
"settings": {
  "title": "设置",
  "subtitle": "数据源连接状态与 GSC 授权（单用户单项目）",
  "noProject": "还没有项目——先在首页创建一次分析。",
  "matrixTitle": "数据源状态",
  "col": { "source": "数据源", "status": "状态", "detail": "详情" },
  "source": {
    "gsc": "Google Search Console",
    "googleCse": "Google 可见性检索 (CSE)",
    "aiProbe": "AI 答案引擎探针",
    "dataforseo": "DataForSEO（关键词/竞品/外链）",
    "render": "渲染抓取 (Cloudflare)",
    "psi": "PageSpeed / CWV",
    "publicCorpora": "公开语料 (Wikipedia/Reddit)"
  },
  "statusConnected": "已连接",
  "statusNotConnected": "未连接",
  "statusAppMissing": "环境未配 OAuth",
  "statusConfigured": "已配置",
  "statusNotConfigured": "未配置",
  "gscTitle": "连接 Google Search Console",
  "connectGsc": "连接 GSC",
  "reconnectGsc": "重新连接 GSC",
  "connectedFlash": "GSC 已连接。请在下方设置站点 URL 以启用关键词采集。",
  "siteUrlLabel": "GSC 站点 URL",
  "siteUrlHint": "填 sc-domain:example.com 或完整 https:// 站点 URL（须与 GSC 中的资源一致）。",
  "saveSiteUrl": "保存站点 URL",
  "siteSaved": "站点 URL 已保存。",
  "siteError": "保存失败，请重试。"
}
```

`messages/en.json`：`common.settingsLink = "Settings"`；顶层加同结构英文（键完全一致）：

```json
"settings": {
  "title": "Settings",
  "subtitle": "Data-source connection status and GSC authorization (single-user, single-project)",
  "noProject": "No project yet — create an analysis on the home page first.",
  "matrixTitle": "Data sources",
  "col": { "source": "Source", "status": "Status", "detail": "Detail" },
  "source": {
    "gsc": "Google Search Console",
    "googleCse": "Google visibility search (CSE)",
    "aiProbe": "AI answer-engine probes",
    "dataforseo": "DataForSEO (keywords/competitors/backlinks)",
    "render": "Render fetch (Cloudflare)",
    "psi": "PageSpeed / CWV",
    "publicCorpora": "Public corpora (Wikipedia/Reddit)"
  },
  "statusConnected": "Connected",
  "statusNotConnected": "Not connected",
  "statusAppMissing": "OAuth app not configured",
  "statusConfigured": "Configured",
  "statusNotConfigured": "Not configured",
  "gscTitle": "Connect Google Search Console",
  "connectGsc": "Connect GSC",
  "reconnectGsc": "Reconnect GSC",
  "connectedFlash": "GSC connected. Set the site URL below to enable keyword collection.",
  "siteUrlLabel": "GSC site URL",
  "siteUrlHint": "Enter sc-domain:example.com or the full https:// site URL (must match the property in GSC).",
  "saveSiteUrl": "Save site URL",
  "siteSaved": "Site URL saved.",
  "siteError": "Save failed, please retry."
}
```

- [ ] **Step 6: 设置页 Server Component**

写入 `app/[locale]/settings/page.tsx`：

```tsx
import { setRequestLocale, getTranslations } from 'next-intl/server'
import { Shell } from '@/components/Shell'
import { getPrimaryProject, getProjectSettings } from '@/lib/repositories'
import { isGscConfigured } from '@/lib/gsc/oauth'
import { buildDataSourceStatuses } from '@/lib/settings/data-sources'
import { SettingsClient } from './SettingsClient'

// 全局单项目设置页。active={1} 仅为 Shell 步进器占位（设置非四步之一，高亮为装饰）。
export default async function SettingsPage({
  params,
  searchParams,
}: {
  params: Promise<{ locale: string }>
  searchParams: Promise<{ gsc?: string }>
}) {
  const { locale } = await params
  const { gsc } = await searchParams
  setRequestLocale(locale)
  const t = await getTranslations('settings')
  const project = await getPrimaryProject()
  if (!project) {
    return (
      <Shell active={1} locale={locale}>
        <section className="screen show">
          <h1 className="text-lg font-semibold">{t('title')}</h1>
          <p className="mt-4 text-sm text-neutral-500">{t('noProject')}</p>
        </section>
      </Shell>
    )
  }
  const settings = await getProjectSettings(project.id)
  const statuses = buildDataSourceStatuses(process.env, {
    gscAppConfigured: isGscConfigured(),
    gscConnected: settings?.gscConnected ?? false,
    gscSiteUrl: settings?.gscSiteUrl ?? null,
  })
  return (
    <Shell active={1} locale={locale} domain={project.domain}>
      <SettingsClient
        projectId={project.id}
        projectDomain={project.domain}
        statuses={statuses}
        gscConnected={settings?.gscConnected ?? false}
        gscSiteUrl={settings?.gscSiteUrl ?? null}
        justConnected={gsc === 'connected'}
      />
    </Shell>
  )
}
```

- [ ] **Step 7: 设置页 Client Component**

写入 `app/[locale]/settings/SettingsClient.tsx`：

```tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { useTranslations } from 'next-intl'
import type { DataSourceStatus } from '@/lib/settings/data-sources'

export function SettingsClient({
  projectId,
  projectDomain,
  statuses,
  gscConnected,
  gscSiteUrl,
  justConnected,
}: {
  projectId: string
  projectDomain: string
  statuses: DataSourceStatus[]
  gscConnected: boolean
  gscSiteUrl: string | null
  justConnected: boolean
}) {
  const t = useTranslations('settings')
  const router = useRouter()
  const [siteUrl, setSiteUrl] = useState(gscSiteUrl ?? `sc-domain:${projectDomain}`)
  const [busy, setBusy] = useState(false)
  const [msg, setMsg] = useState<string | null>(justConnected ? t('connectedFlash') : null)

  function connectGsc() {
    window.location.href = `/api/gsc/auth?projectId=${encodeURIComponent(projectId)}`
  }
  async function saveSiteUrl() {
    setBusy(true)
    const res = await fetch('/api/gsc/site', {
      method: 'POST',
      headers: { 'content-type': 'application/json' },
      body: JSON.stringify({ projectId, siteUrl: siteUrl.trim() }),
    })
    setBusy(false)
    setMsg(res.ok ? t('siteSaved') : t('siteError'))
    if (res.ok) router.refresh()
  }

  function statusText(s: DataSourceStatus): string {
    if (s.key === 'gsc') return s.connected ? t('statusConnected') : s.configured ? t('statusNotConnected') : t('statusAppMissing')
    return s.configured ? t('statusConfigured') : t('statusNotConfigured')
  }

  return (
    <section className="screen show" style={{ maxWidth: 760 }}>
      <h1 className="text-lg font-semibold">{t('title')}</h1>
      <p className="mt-1 text-sm text-neutral-500">{t('subtitle')}</p>
      {msg && <p role="status" className="mt-2 text-sm" style={{ color: '#b45309' }}>{msg}</p>}

      <h2 className="mt-6 text-sm font-medium">{t('matrixTitle')}</h2>
      <div className="report-table-wrap mt-2">
        <table className="report-table">
          <thead>
            <tr><th>{t('col.source')}</th><th>{t('col.status')}</th><th>{t('col.detail')}</th></tr>
          </thead>
          <tbody>
            {statuses.map((s) => (
              <tr key={s.key}>
                <td>{t(`source.${s.key}`)}</td>
                <td>{statusText(s)}</td>
                <td className="mono">{s.detail ?? '—'}</td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <h2 className="mt-6 text-sm font-medium">{t('gscTitle')}</h2>
      <button type="button" onClick={connectGsc} disabled={busy}>
        {gscConnected ? t('reconnectGsc') : t('connectGsc')}
      </button>
      {gscConnected && (
        <div className="mt-3">
          <label className="block text-sm">
            {t('siteUrlLabel')}
            <input className="mono ml-2" value={siteUrl} onChange={(e) => setSiteUrl(e.target.value)} />
          </label>
          <p className="mt-1 text-xs text-neutral-500">{t('siteUrlHint')}</p>
          <button type="button" className="mt-2" onClick={saveSiteUrl} disabled={busy || !siteUrl.trim()}>
            {t('saveSiteUrl')}
          </button>
        </div>
      )}
    </section>
  )
}
```

- [ ] **Step 8: tsc + lint + build + 全量测试**

Run: `npx tsc --noEmit && npm run lint && npm test && npm run build`
Expected: 0 错 / 0 error / 全绿 / build 通过，`/[locale]/settings` 进路由清单。

手动（可选）：`npm run dev` → `/zh/settings`，确认状态矩阵渲染；无项目显空态；连 GSC 后显站点 URL 表单。

- [ ] **Step 9: Commit**

```bash
git add lib/repositories/index.ts app/api/gsc/site app/api/gsc/callback/route.ts components/Shell.tsx "app/[locale]/settings" messages/en.json messages/zh.json
git commit -m "feat(phase-b-ui): 设置页——数据源状态矩阵 + GSC 连接/站点 URL + Shell 入口 + callback 改向"
```

---

## Task 4: KeywordTable 抽取 + 报告页重构

**Files:**
- Create: `components/KeywordTable.tsx`
- Modify: `app/[locale]/runs/[id]/report/page.tsx:387-452`

**Interfaces:**
- Produces: `KeywordTable`（async Server Component）+ 类型 `KeywordMetricRow`、`KeywordGapRow`、`KeywordTextMap`。
- 契约：`<KeywordTable keywordMetrics={...} keywordGaps={...} keywordText={...} />`；组件内 `getTranslations('report')` 用 `report.keywords.*` 列标签，空态渲染 `report.keywords.empty`。
- Consumes（下游）：Task 5 关键词 tab 复用。

- [ ] **Step 1: 抽取 KeywordTable 组件**

写入 `components/KeywordTable.tsx`（逐字搬运报告 §4 的 metrics/gaps/空态渲染，列标签仍读 `report` 命名空间）：

```tsx
import { getTranslations } from 'next-intl/server'

export interface KeywordMetricRow {
  id: string
  keywordId: string
  clicks: number | null
  impressions: number | null
  ctr: string | null
  position: string | null
  source: string
}
export interface KeywordGapRow {
  id: string
  keywordId: string
  gapType: string
  ourPosition: string | null
  opportunityScore: string | null
}
export type KeywordTextMap = Map<string, { text: string; volume: number | null; difficulty: number | null }>

// 关键词表（metrics + gaps）。报告页 §4 与关键词现状 tab 同用。列标签复用 report.keywords.*。
export async function KeywordTable({
  keywordMetrics,
  keywordGaps,
  keywordText,
}: {
  keywordMetrics: KeywordMetricRow[]
  keywordGaps: KeywordGapRow[]
  keywordText: KeywordTextMap
}) {
  const t = await getTranslations('report')
  if (!keywordMetrics.length && !keywordGaps.length) {
    return <p className="note">{t('keywords.empty')}</p>
  }
  return (
    <>
      <p className="note">{t('keywords.estimateNote')}</p>
      {keywordMetrics.length ? (
        <div className="report-table-wrap">
          <table className="report-table">
            <thead>
              <tr>
                <th>{t('keywords.col.keyword')}</th>
                <th>{t('keywords.col.clicks')}</th>
                <th>{t('keywords.col.impressions')}</th>
                <th>{t('keywords.col.ctr')}</th>
                <th>{t('keywords.col.position')}</th>
                <th>{t('keywords.col.source')}</th>
              </tr>
            </thead>
            <tbody>
              {keywordMetrics.map((m) => (
                <tr key={m.id}>
                  <td>{keywordText.get(m.keywordId)?.text ?? m.keywordId}</td>
                  <td>{m.clicks ?? '—'}</td>
                  <td>{m.impressions ?? '—'}</td>
                  <td>{m.ctr ?? '—'}</td>
                  <td>{m.position ?? '—'}</td>
                  <td className="mono">{m.source}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ) : null}
      {keywordGaps.length ? (
        <div className="report-table-wrap">
          <h4>{t('keywords.gapsTitle')}</h4>
          <table className="report-table">
            <thead>
              <tr>
                <th>{t('keywords.col.keyword')}</th>
                <th>{t('keywords.col.gapType')}</th>
                <th>{t('keywords.col.ourPosition')}</th>
                <th>{t('keywords.col.opportunity')}</th>
                <th>{t('keywords.col.volume')}</th>
              </tr>
            </thead>
            <tbody>
              {keywordGaps.map((gp) => (
                <tr key={gp.id}>
                  <td>{keywordText.get(gp.keywordId)?.text ?? gp.keywordId}</td>
                  <td>{t(`keywords.gapType.${gp.gapType}`)}</td>
                  <td>{gp.ourPosition ?? '—'}</td>
                  <td>{gp.opportunityScore ?? '—'}</td>
                  <td>{keywordText.get(gp.keywordId)?.volume ?? '—'}</td>
                </tr>
              ))}
            </tbody>
          </table>
        </div>
      ) : null}
    </>
  )
}
```

- [ ] **Step 2: 报告页 §4 改调组件**

`app/[locale]/runs/[id]/report/page.tsx` 顶部 import：`import { KeywordTable } from '@/components/KeywordTable'`。

把 388-451 段的 `<section id="sec-keywords">` 内部——即 388 行 `<section>`、389 `<h3>` 之后，390-451 的 `{keywordMetrics.length || keywordGaps.length ? (...) : (<p className="note">{t('keywords.empty')}</p>)}` 整块——替换为：

```tsx
          <section id="sec-keywords" className="report-section">
            <h3>{t('toc.keywords')}</h3>
            <KeywordTable keywordMetrics={keywordMetrics} keywordGaps={keywordGaps} keywordText={keywordText} />
          </section>
```

（`keywordMetrics`/`keywordGaps`/`keywordText` 都是该页已加载/构造好的变量；行结构与组件入参类型结构兼容——多余字段不影响赋值。）

- [ ] **Step 3: tsc + 报告相关测试 + 全量**

Run: `npx tsc --noEmit && npm test`
Expected: tsc 0；全量绿（KeywordTable 是等价重构，报告页现有测试若断言关键词区渲染应仍通过；若有快照需更新则更新）。

- [ ] **Step 4: build 验证渲染**

Run: `npm run build`
Expected: 通过，report 路由正常。

- [ ] **Step 5: Commit**

```bash
git add components/KeywordTable.tsx "app/[locale]/runs/[id]/report/page.tsx"
git commit -m "refactor(phase-b-ui): 抽取 KeywordTable 组件，报告页 §4 改调（为关键词 tab 复用铺路）"
```

---

## Task 5: 关键词现状 tab + Screen2 链接 + keywords i18n

**Files:**
- Create: `app/[locale]/runs/[id]/keywords/page.tsx`
- Modify: `app/[locale]/runs/[id]/page.tsx:139-146`
- Modify: `messages/en.json`、`messages/zh.json`

**Interfaces:**
- Consumes: Task 4 `KeywordTable`；`getRun`、`getProject`、`getRunKeywordMetrics`、`getRunKeywordGaps`、`getKeywords`。

- [ ] **Step 1: i18n 加 keywords 命名空间 + screen2.keywordsLink（en + zh 锁步）**

`messages/zh.json`：`screen2` 内加 `"keywordsLink": "关键词现状 →"`；顶层加：

```json
"keywords": {
  "title": "关键词现状",
  "subtitle": "GSC 实测关键词指标（L4）与 DataForSEO 缺口分析。",
  "empty": "本次诊断暂无关键词数据。",
  "emptyCta": "去设置页连接 GSC / 配置 DataForSEO →"
}
```

`messages/en.json`：`screen2.keywordsLink = "Keywords →"`；顶层加：

```json
"keywords": {
  "title": "Keyword status",
  "subtitle": "GSC-measured keyword metrics (L4) and DataForSEO gap analysis.",
  "empty": "No keyword data for this run yet.",
  "emptyCta": "Go to Settings to connect GSC / configure DataForSEO →"
}
```

- [ ] **Step 2: 关键词 tab 页**

写入 `app/[locale]/runs/[id]/keywords/page.tsx`：

```tsx
import { getTranslations, setRequestLocale } from 'next-intl/server'
import { notFound } from 'next/navigation'
import Link from 'next/link'
import { Shell } from '@/components/Shell'
import { KeywordTable } from '@/components/KeywordTable'
import { getRun, getProject, getRunKeywordMetrics, getRunKeywordGaps, getKeywords } from '@/lib/repositories'

// 关键词现状 tab（Screen 2 子页，active={2}）。复用 KeywordTable。
export default async function KeywordsPage({
  params,
}: {
  params: Promise<{ locale: string; id: string }>
}) {
  const { locale, id } = await params
  setRequestLocale(locale)
  const [t, run] = await Promise.all([getTranslations('keywords'), getRun(id)])
  if (!run) notFound()
  const [project, keywordMetrics, keywordGaps, keywords] = await Promise.all([
    getProject(run.projectId),
    getRunKeywordMetrics(id),
    getRunKeywordGaps(id),
    getKeywords(run.projectId),
  ])
  const keywordText = new Map(
    keywords.map((k) => [k.id, { text: k.text, volume: k.searchVolume, difficulty: k.difficulty }]),
  )
  const isEmpty = !keywordMetrics.length && !keywordGaps.length
  return (
    <Shell active={2} locale={locale} runId={id} domain={project?.domain}>
      <section className="screen show">
        <h1 className="text-lg font-semibold">
          {project?.domain} · {t('title')}
        </h1>
        <p className="mt-1 text-sm text-neutral-500">{t('subtitle')}</p>
        {isEmpty ? (
          <p className="mt-4 text-sm">
            {t('empty')}{' '}
            <Link href={`/${locale}/settings`} className="underline underline-offset-2">
              {t('emptyCta')}
            </Link>
          </p>
        ) : (
          <div className="mt-4">
            <KeywordTable keywordMetrics={keywordMetrics} keywordGaps={keywordGaps} keywordText={keywordText} />
          </div>
        )}
      </section>
    </Shell>
  )
}
```

- [ ] **Step 3: Screen 2 加 keywordsLink**

`app/[locale]/runs/[id]/page.tsx` 的 `flex gap-4` 链接行（140-145）加第三个链接：

```tsx
            <div className="flex gap-4">
              <Link href={`/${locale}/runs/${id}/site`} className="text-sm underline underline-offset-2">
                {t('screen2.siteLink')}
              </Link>
              <Link href={`/${locale}/runs/${id}/keywords`} className="text-sm underline underline-offset-2">
                {t('screen2.keywordsLink')}
              </Link>
              <Link href={`/${locale}/runs/${id}/competitors`} className="text-sm underline underline-offset-2">
                {t('screen2.competitorsLink')}
              </Link>
            </div>
```

- [ ] **Step 4: tsc + lint + 全量 + build**

Run: `npx tsc --noEmit && npm run lint && npm test && npm run build`
Expected: 0 / 0 error / 全绿 / build 通过，`/[locale]/runs/[id]/keywords` 进路由清单。

手动（可选）：`/zh/runs/<id>/keywords` —— 有数据渲染表、无数据显 CTA；Screen 2 链接行出现「关键词现状」。

- [ ] **Step 5: Commit**

```bash
git add "app/[locale]/runs/[id]/keywords" "app/[locale]/runs/[id]/page.tsx" messages/en.json messages/zh.json
git commit -m "feat(phase-b-ui): 关键词现状 tab（复用 KeywordTable）+ Screen2 入口链接"
```

---

## Self-Review 记录

**Spec 覆盖核对（对 `2026-07-06-phase-b-ui-design.md`）：**
- §2 设置页（Shell 入口 + 状态矩阵 + GSC 连接 + 站点 URL 表单 + callback 改向 + getPrimaryProject）→ Task 2+3 ✅
- §3 关键词 tab（runs/[id]/keywords + Screen2 链接 + KeywordTable 抽取 + 空态 CTA）→ Task 4+5 ✅
- §4 avgRank 通真（impressionWeightedAvgPosition + 采集器 payload + deriveAvgRank 择证据）→ Task 1 ✅
- §5 文件边界 → File Structure 对齐 ✅
- §8 不做项（sites.list 自动发现 / 可编辑 project_settings / DataForSEO key UI / 延后规则 / 多项目）→ 计划内未越界 ✅

**Placeholder 扫描：** Task 3 Step 3（callback redirect）以「改这一个 URL 字符串」描述而非贴整文件——因当前 callback 源码未逐字持有，且只需替换一个已知目标字符串（`/settings?gsc=connected`），实现者读文件即可精确定位；其余步骤均含完整代码。无 TBD/TODO。

**类型一致性：** `DataSourceStatus`（Task 2）= 设置页/SettingsClient 消费（Task 3）；`KeywordMetricRow`/`KeywordGapRow`/`KeywordTextMap`（Task 4）= 报告页与关键词 tab 传入的 Drizzle 行结构兼容（keyword_metrics: clicks/impressions integer nullable、ctr/position text nullable、source notNull；keyword_gaps: gapType notNull、ourPosition/opportunityScore text nullable；keywordText: searchVolume/difficulty integer nullable）；`impressionWeightedAvgPosition` 入参 `{impressions,position}` 与 `GscRow` 兼容；`getPrimaryProject`/`setGscSiteUrl`（Task 3）签名与设置页/site 路由调用一致。

**已知装饰性取舍（记 final triage）：** 设置页 `Shell active={1}` 使步进器高亮「新建分析」（设置非四步之一）——V0 可接受，如需可后续给 Shell.active 加可选态。
```
